# Confirmation Subsystem

This folder will contain minimal frontend/UI components for user confirmation flows. It is intended for ambiguous cases only and must never block core book creation or library usage.
