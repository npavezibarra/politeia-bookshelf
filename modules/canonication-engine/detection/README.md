# Detection Subsystem

This folder will host cron-driven detection logic that scans canonical records and identifies likely duplicates. Its role is to generate merge candidates only, without enforcing any changes or blocking user flows.
