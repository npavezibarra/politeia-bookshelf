# Relinking Subsystem

This folder will hold transactional logic to safely reassign User Books and migrate dependent entities. It focuses on preserving user data while removing redundant canonical records after safe relinks.
